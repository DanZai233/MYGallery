═══════════════════════════════════════════════════════════════
   🚀 立即发布到 Docker Hub
═══════════════════════════════════════════════════════════════

仓库: DanZai233/mygallery

═══════════════════════════════════════════════════════════════
✅ 已完成的准备工作
═══════════════════════════════════════════════════════════════

1. ✅ Dockerfile 已修复（Go 1.23 + GOTOOLCHAIN=auto）
2. ✅ workflow 已配置（IMAGE_NAME = DanZai233/mygallery）
3. ✅ .dockerignore 已优化

═══════════════════════════════════════════════════════════════
📋 发布步骤（3步）
═══════════════════════════════════════════════════════════════

第 1 步: 配置 Docker Hub 访问令牌
─────────────────────────────────────────
1. 访问 https://hub.docker.com/
2. 登录你的账号
3. Account Settings → Security → New Access Token
4. 描述: GitHub Actions
5. 权限: Read, Write, Delete
6. Generate 并复制令牌（⚠️ 只显示一次）

第 2 步: 添加 GitHub Secrets
─────────────────────────────────────────
访问: https://github.com/DanZai233/mygallery/settings/secrets/actions

添加 2 个 Secrets:

Secret 1:
  Name: DOCKER_USERNAME
  Value: DanZai233

Secret 2:
  Name: DOCKER_PASSWORD
  Value: [粘贴刚才复制的令牌]

第 3 步: 推送代码并发布
─────────────────────────────────────────
cd /root/MYGallery

# 提交修复
git add Dockerfile .dockerignore
git commit -m "fix: 更新 Dockerfile Go 版本到 1.23"
git push origin main

# 发布新版本
bash scripts/release.sh

# 选择版本类型（建议选 3 = patch）
# 确认后自动推送，触发 GitHub Actions

═══════════════════════════════════════════════════════════════
👀 查看构建进度
═══════════════════════════════════════════════════════════════

访问: https://github.com/DanZai233/mygallery/actions

状态:
  🟡 黄色 = 正在构建（等待 5-10 分钟）
  ✅ 绿色 = 构建成功
  ❌ 红色 = 构建失败

构建成功后会生成以下标签:
  • DanZai233/mygallery:1.0.1  (精确版本)
  • DanZai233/mygallery:1.0    (次版本)
  • DanZai233/mygallery:1      (主版本)
  • DanZai233/mygallery:latest (最新版本)

═══════════════════════════════════════════════════════════════
🎉 使用发布的镜像
═══════════════════════════════════════════════════════════════

# 拉取镜像
docker pull DanZai233/mygallery:latest

# 运行容器
docker run -d \
  --name mygallery \
  -p 8080:8080 \
  -v $(pwd)/config.yaml:/app/config.yaml \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/uploads:/app/uploads \
  DanZai233/mygallery:latest

# 访问应用
http://localhost:8080

═══════════════════════════════════════════════════════════════
🐛 如果构建失败
═══════════════════════════════════════════════════════════════

1. 检查 Secrets 配置是否正确
   名称: DOCKER_USERNAME, DOCKER_PASSWORD（大写）
   
2. 检查 Docker Hub 访问令牌
   权限需要包含: Read, Write, Delete
   
3. 查看 Actions 日志
   点击失败的 workflow 查看详细错误

4. 重新运行
   Actions → 失败的 workflow → Re-run all jobs

═══════════════════════════════════════════════════════════════

配置好 Secrets 后，就可以自动构建和发布了！🎊

═══════════════════════════════════════════════════════════════
